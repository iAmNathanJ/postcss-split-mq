import test from 'ava';
import postcss from 'postcss';
import postcssSplitMq from '../src/main';
import { read } from '../lib/io';

test.skip('writes sourcemaps', async t => {
  t.fail();
});

test.skip('does not break sourcemaps', async t => {
  t.fail();
});
